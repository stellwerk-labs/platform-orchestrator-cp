services:
  nats:
    image: nats:2.11-alpine
    command: ["--jetstream", "--store_dir", "/data", "--http_port", "8222", "--auth", "integration-token"]
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:8222/healthz | grep -q ok"]
      interval: 2s
      timeout: 5s
      retries: 15
  platform-orchestrator-dp-platform-orchestrator-dp:
    environment:
      NATS_BOOTSTRAP: "true"
      LOG_LEVEL: debug
      DD_AGENT_HOST: ""
      SHUTDOWN_DELAY: 1s
      DEPLOYMENTS_COMPLETED_BEFORE: 10s
      VAULT_AUTH: token:mytoken
    depends_on:
      nats:
        condition: service_healthy
      vault:
        condition: service_started
  platform-orchestrator-cp-platform-orchestrator-cp:
    environment:
      DD_AGENT_HOST: ""
      SHUTDOWN_DELAY: 1s
      LOG_LEVEL: debug
      VAULT_AUTH: token:mytoken
      NATS_BOOTSTRAP_STREAMS: "true"
    depends_on:
      nats:
        condition: service_healthy
      vault:
        condition: service_started
      platform-orchestrator-iam-platform-orchestrator-iam:
        condition: service_started
  platform-orchestrator-iam-platform-orchestrator-iam:
    environment:
      LOG_LEVEL: debug
      DD_AGENT_HOST: ""
      SHUTDOWN_DELAY: 1s
    depends_on:
      spicedb:
        condition: service_healthy
  vault:
    image: hashicorp/vault:1.14
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: mytoken
      VAULT_DEV_LISTEN_ADDRESS: 0.0.0.0:8200
    cap_add:
      - IPC_LOCK
    ports:
      - "8200:8200"
  spicedb:
    image: authzed/spicedb:v1.45.4
    command: serve-testing
    ports:
      - "50051:50051"
      - "8443:8443"
      - "9090:9090"  
    environment:
      - SPICEDB_GRPC_PRESHARED_KEY=t0k3n
    healthcheck:
      test: ["CMD", "grpc_health_probe", "-v", "-addr=localhost:50051"]
      interval: 5s
      timeout: 3s
      retries: 3

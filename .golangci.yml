# see https://golangci-lint.run/usage/configuration/
version: "2"
linters:
  default: standard
  enable:
    - bodyclose
    - contextcheck
    - goconst
    - gosec
    - sqlclosecheck
    - testifylint

  exclusions:
    rules:
      - path: '_test\.go$'
        linters:
          - goconst
